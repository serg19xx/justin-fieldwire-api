<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to FieldWire</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f8f9fa;
        }
        .container {
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        .header {
            background-color: #2563eb;
            color: white;
            padding: 40px 30px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: 600;
        }
        .content {
            padding: 30px;
        }
        .greeting {
            font-size: 18px;
            margin-bottom: 20px;
            color: #1f2937;
        }
        .job-title {
            background-color: #f3f4f6;
            padding: 15px;
            border-radius: 6px;
            margin: 20px 0;
            text-align: center;
            border-left: 4px solid #2563eb;
        }
        .credentials {
            background-color: #f9fafb;
            border: 1px solid #e5e7eb;
            border-radius: 6px;
            padding: 20px;
            margin: 25px 0;
        }
        .credentials h3 {
            margin: 0 0 15px 0;
            color: #374151;
            font-size: 16px;
        }
        .credential-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 8px 0;
            border-bottom: 1px solid #e5e7eb;
        }
        .credential-row:last-child {
            border-bottom: none;
        }
        .credential-label {
            font-weight: 500;
            color: #6b7280;
        }
        .credential-value {
            font-family: 'Monaco', 'Menlo', monospace;
            background-color: #ffffff;
            padding: 4px 8px;
            border-radius: 4px;
            border: 1px solid #d1d5db;
            font-size: 14px;
        }
        .cta-section {
            text-align: center;
            margin: 30px 0;
        }
        .cta-button {
            display: inline-block;
            background-color: #2563eb;
            color: #ffffff;
            text-decoration: none;
            padding: 12px 24px;
            border-radius: 6px;
            font-weight: 500;
            font-size: 16px;
        }
        .steps {
            background-color: #f0f9ff;
            border: 1px solid #bae6fd;
            border-radius: 6px;
            padding: 20px;
            margin: 25px 0;
        }
        .steps h4 {
            margin: 0 0 15px 0;
            color: #0369a1;
            font-size: 16px;
        }
        .steps ol {
            margin: 0;
            padding-left: 20px;
        }
        .steps li {
            margin: 8px 0;
            color: #0c4a6e;
        }
        .expiry-notice {
            background-color: #fef3c7;
            border: 1px solid #f59e0b;
            border-radius: 6px;
            padding: 15px;
            margin: 25px 0;
            text-align: center;
        }
        .expiry-notice strong {
            color: #92400e;
        }
        .footer {
            background-color: #f9fafb;
            padding: 20px 30px;
            text-align: center;
            border-top: 1px solid #e5e7eb;
        }
        .footer p {
            margin: 5px 0;
            color: #6b7280;
            font-size: 14px;
        }
        .footer-links {
            margin: 15px 0;
        }
        .footer-links a {
            color: #2563eb;
            text-decoration: none;
            margin: 0 10px;
        }
        {% if attemptNumber > 1 %}
        .reminder-notice {
            background-color: #fef2f2;
            border: 1px solid #fca5a5;
            border-radius: 6px;
            padding: 15px;
            margin: 20px 0;
            text-align: center;
        }
        .reminder-notice strong {
            color: #dc2626;
        }
        {% endif %}
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Welcome to FieldWire</h1>
        </div>
        
        <div class="content">
            <div class="greeting">
                Hello {{ firstName }} {{ lastName }},
            </div>
            
            <p>You've been invited to join our team as a <strong>{{ jobTitle }}</strong>.</p>
            
            {% if attemptNumber > 1 %}
            <div class="reminder-notice">
                <strong>Reminder #{{ attemptNumber - 1 }}:</strong> This is a friendly reminder about your invitation to join FieldWire.
            </div>
            {% endif %}
            
            <div class="credentials">
                <h3>Your Login Credentials</h3>
                <div class="credential-row">
                    <span class="credential-label">Email:</span>
                    <span class="credential-value">{{ email }}</span>
                </div>
                <div class="credential-row">
                    <span class="credential-label">Temporary Password:</span>
                    <span class="credential-value">{{ tempPassword }}</span>
                </div>
            </div>
            
            <div class="cta-section">
                <a href="{{ loginUrl }}" class="cta-button" style="display: inline-block; background-color: #2563eb; color: #ffffff !important; text-decoration: none; padding: 12px 24px; border-radius: 6px; font-weight: 500; font-size: 16px;">Login to Your Account</a>
            </div>
            
            <div class="steps">
                <h4>Next Steps:</h4>
                <ol>
                    <li>Click the login button above</li>
                    <li>Use your temporary password to sign in</li>
                    <li>Set your own secure password</li>
                    <li>Start exploring the platform</li>
                </ol>
            </div>
            
            <div class="expiry-notice">
                <strong>Important:</strong> This invitation expires in <strong>{{ expiryHours }} hours</strong> ({{ expiryDate }}).
            </div>
            
            <p>If you have any questions, please contact our support team.</p>
        </div>
        
        <div class="footer">
            <p><strong>Best regards,</strong></p>
            <p>The FieldWire Team</p>
            <div class="footer-links">
                <a href="{{ appUrl }}">Visit Platform</a> |
                <a href="mailto:support@fieldwire.com">Contact Support</a>
            </div>
            <p style="font-size: 12px; margin-top: 15px;">
                This invitation was sent to {{ email }}. If you didn't expect this invitation, please ignore this email.
            </p>
        </div>
    </div>
</body>
</html>
